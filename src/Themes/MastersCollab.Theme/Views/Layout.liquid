<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ Model.Title | default: "Master's Collaboration App" }}</title>

    <!-- Custom Styling -->
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f3f4f6;
            color: #1a202c;
        }

        /* Static Banner Section */
        .banner {
            background-color: #4a5568;
            color: white;
            padding: 40px 20px;
            text-align: center;
            font-size: 1.5rem;
        }

        /* Navigation Bar */
        .navbar {
            background-color: #2d3748;
            display: flex;
            justify-content: center;
            padding: 10px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        .navbar a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-size: 1rem;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .navbar a:hover {
            color: #63b3ed;
        }

        /* Main Content Section */
        .content {
            padding: 20px;
            max-width: 800px;
            margin: 20px auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4a90e2;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s ease;
            margin: 15px 0;
        }

        .btn:hover {
            background-color: #357ab7;
        }

        /* Footer */
        footer {
            background-color: #2d3748;
            color: white;
            text-align: center;
            padding: 10px 0;
            margin-top: 30px;
            font-size: 0.9rem;
        }

        footer a {
            color: #63b3ed;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <!-- Banner Section -->
    <div class="banner">
        <h1>Welcome to the Collaboration Portal</h1>
        <p>Connect and Learn!</p>
    </div>

    <!-- Navigation Bar -->
    <div class="navbar">
        <a href="/blog">Blog</a>
        <a href="/forum">Forum</a>
        <a href="/courses">Courses</a>
        <a href="/projects">Projects</a>
        <a href="/contact-us">Contact Us</a>
    </div>

    <!-- Main Content Area -->
    <div class="content">
        {% if Request.Path == '/forum' %}
            <a href="/Admin/Contents/ContentTypes/ForumPost/Create" class="btn btn-primary">
                Create New Forum Post
            </a>
        {% endif %}
        {% if Request.Path == '/courses' %}
            <a href="/Admin/Contents/ContentTypes/Course/Create" class="btn btn-primary">
                Submit New Course
            </a>
        {% endif %}
        {% if Request.Path == '/projects' %}
            <a href="/Admin/Contents/ContentTypes/Project/Create" class="btn btn-primary">
                Submit New Project
            </a>
        {% endif %}
        {{ Model.Content }}
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Collaboration Portal. All Rights Reserved.</p>
    </footer>

</body>
</html>
